## No Specification Changes

This is a rollback change that removes a custom implementation and restores the previous standard VitePress configuration. No new functional requirements are being added, and no existing capability specifications are being modified.

### Changes Summary

- **Removed**: Custom `Home.vue` component (UI implementation detail, not a capability)
- **Removed**: `@heroicons/vue` dependency (implementation detail)
- **Modified**: `docs/index.md` and `docs/zh/index.md` frontmatter configuration (content change, not behavioral)

All changes are related to documentation presentation and do not affect system behavior, APIs, or user-facing capabilities.
